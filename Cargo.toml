[package]
name = "cuda-a2a"
version = "0.1.0"
edition = "2021"
description = "Agent-to-Agent protocol — deliberative messaging with confidence propagation, trust scoring, and fleet coordination"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
